{% extends 'base-staff.html.twig' %}
{% block title %}Gestion des Feedbacks{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="card shadow-lg border-0">
        <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
            <h2 class="h5"><i class="fas fa-comments"></i> Gestion des Feedbacks</h2>
            <link rel="stylesheet" href="{{ asset('styles/listef.css') }}">
        </div>
        <div class="card-body">
            <p class="text-center">Cette page est obsolète pour les administrateurs. Veuillez utiliser la <a href="{{ path('admin_liste_feedbacks') }}">liste des voyages</a> pour consulter les feedbacks.</p>
        </div>
    </div>
</div>
{% endblock %}